# Drawing Intelligence

## Run locally

Open `index.html` with VS Code Live Server. Each Plot click downloads a numbered card-position JSON file. Move downloaded snapshots into `cards/json/`; on refresh, the highest-numbered snapshot is loaded.